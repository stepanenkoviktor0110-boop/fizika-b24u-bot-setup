# Snapshot перед переходом промпта на логику (без синонимов)

**Дата:** 2026-05-24 04:55
**Действие:** PATCH widget_settings.system_prompt через PostgREST + DELETE training_data id=2e83c642 (избыточная KB-карточка).

**Текущий промпт (3422 знака):** скопирован из БД, доступен через git log по предыдущему snapshot `2026-05-23-before-prompt-edit.md` (там полный текст) + правки из коммита `2d34f39`.

**Зачем:** правило «один критерий → slice-URL» сейчас живёт в KB-карточке. RAG может её не вернуть в Top-N → бот не применит правило. Правила поведения должны быть в системном промпте (всегда в контексте), KB — для данных/фактов клиента.

**Что меняется:**
- Перенос правила «один критерий → slice-URL» из KB в промпт, секция «АЛГОРИТМ» → «ЛОГИКА ОТВЕТА».
- Заменить «триггеры-формулировки» («что есть», «варианты», «ассортимент») на структурные правила по комбинации параметров запроса (ЖК / комнатность / бюджет / площадь / атрибут).
- Добавить формулу slice-URL в СПРАВОЧНИК — чтобы бот сам собирал URL под любые числа.
- Удалить избыточную KB-карточку id=2e83c642 (она дублирует логику промпта).

**Что сохраняется:**
- KB add7637b — про White Box (данные о терминологии конкретного ЖК, не правило).
- Старые slice-карточки (готовые URL для топ-10 типичных запросов) — оставить для подстраховки.
- Все каталог-карточки, FAQ, СПОСОБЫ ОПЛАТЫ, О Fizika.
